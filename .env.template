# PostgreSQL Aiven (remplacer par tes infos Aiven)
DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=require

# Clé secrète (générer avec `openssl rand -base64 32`)
NEXTAUTH_SECRET=your-long-secret-key

# Clé secrète pour BetterAuth
BETTER_AUTH_SECRET=your-long-secret-key

# URL du site (utilisé pour les callbacks auth, liens magic, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email SMTP config (Mailtrap)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=xxxxxxxxxxxx
SMTP_PASS=xxxxxxxxxxxx
FROM_EMAIL=no-reply@tjm.sabx.fr